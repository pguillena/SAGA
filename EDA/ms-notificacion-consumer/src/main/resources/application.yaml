spring:
  application:
    name: ms-notificacion-consumer

  mail:
    host: smtp.office365.com
    port: 587
    username: notificaciones@pcm.gob.pe
    password: "0GT1.PCM.N0T1Fi@C10N"
    protocol: smtp
    tls: true
    from: notificaciones@pcm.gob.pe
    properties:
      mail:
        smtp:
          starttls:
            enable: true
          ssl:
            trust: smtp.office365.com

  rabbitmq:
    host: 192.168.18.39
    port: 5672
    username: guest
    password: guest

  amqp:
    deserialization:
      trust:
        all: true


rabbitmq:
  queue:
    proceso:
      notificacion: queue_proceso_notificacion



server:
  port: 8085

logging:
  level:
    org.hibernate.SQL: DEBUG

springdoc:
  api-docs:
    enable: true
    path: /api/docs
  swagger-ui:
    enable: true
    path: /swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: "*"
      health:
        show-details: always
